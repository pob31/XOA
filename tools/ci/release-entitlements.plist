<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Entitlements the release .app is re-signed with (tools/ci/build-macos-release.sh).

  get-task-allow MUST be false: a build that carries it true is rejected by the
  notary service with statusCode 4000. The CMake/Makefiles build does not inject
  it the way the Projucer's Xcode exporter does, but the re-sign pins it anyway
  so an `-G Xcode` release build cannot quietly reintroduce it.

  audio-input      microphone/input-device access (the Ambisonics encoder's live inputs).
  network.client   OSC send, MCP client connections.
  network.server   the OSC receive port and the MCP/websocket server.

  disable-library-validation: the app dlopens the webcam head-tracker plugin
  (libwfs_headtrack.dylib) from beside the executable. That plugin is NOT bundled
  in the release (see Documentation/XOA-RELEASE.md), so any copy present is one the
  user built locally and is signed by a different team - or not at all. Without
  this, the hardened runtime refuses to load it. No camera entitlement is claimed:
  the app itself never opens a camera, the plugin does, and it has its own.
-->
<plist version="1.0">
<dict>
    <key>com.apple.security.get-task-allow</key>
    <false/>
    <key>com.apple.security.device.audio-input</key>
    <true/>
    <key>com.apple.security.network.client</key>
    <true/>
    <key>com.apple.security.network.server</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
</dict>
</plist>
